'use client';
import { AlertTriangle } from 'lucide-react';
import { Badge } from '@/components/ui/badge';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { useOutstandingInvoicesQuery } from '@/queries/invoice.queries';
function money(v: unknown) { return `${Number(v ?? 0).toLocaleString()} ETB`; }
export default function Page() { const q = useOutstandingInvoicesQuery(); const all = q.data?.data ?? []; const rows = all.filter((i) => i.due_date && new Date(i.due_date) < new Date() && Number(i.balance ?? 0) > 0); return <div className="space-y-6"><div><h1 className="text-2xl font-semibold">Overdue Invoices</h1><p className="text-sm text-muted-foreground">Track invoices past due date and follow up customer credit settlement.</p></div><Card><CardHeader><CardTitle className="flex items-center gap-2"><AlertTriangle className="h-5 w-5"/>Overdue receivables</CardTitle></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>Invoice</TableHead><TableHead>Customer</TableHead><TableHead>Due date</TableHead><TableHead>Total</TableHead><TableHead>Paid</TableHead><TableHead>Balance</TableHead><TableHead>Status</TableHead></TableRow></TableHeader><TableBody>{q.isLoading ? <TableRow><TableCell colSpan={7}>Loading overdue invoices...</TableCell></TableRow> : rows.length === 0 ? <TableRow><TableCell colSpan={7}>No overdue invoices found.</TableCell></TableRow> : rows.map((i) => <TableRow key={i.id}><TableCell>{i.invoice_number ?? i.id}</TableCell><TableCell>{i.customer?.name ?? i.customer_id}</TableCell><TableCell>{i.due_date}</TableCell><TableCell>{money(i.total)}</TableCell><TableCell>{money(i.paid_amount)}</TableCell><TableCell>{money(i.balance)}</TableCell><TableCell><Badge variant="destructive">overdue</Badge></TableCell></TableRow>)}</TableBody></Table></CardContent></Card></div>; }
